import { NextResponse } from "next/server";
import { env } from "@/server/env";
export const GET = async () => { const base = env.APP_URL.replace(/\/$/, ""); return NextResponse.json({ issuer: base, authorization_endpoint: `${base}/oauth/authorize`, token_endpoint: `${base}/oauth/token`, registration_endpoint: `${base}/oauth/register`, response_types_supported: ["code"], grant_types_supported: ["authorization_code"], code_challenge_methods_supported: ["S256"], token_endpoint_auth_methods_supported: ["none"], scopes_supported: ["tally.read", "tally.time.write", "tally.expenses", "tally.approvals", "tally.admin"] }, { headers: { "cache-control": "public, max-age=3600" } }); };
